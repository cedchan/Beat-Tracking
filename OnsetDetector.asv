classdef OnsetDetector
    methods (Static)
        
        % Inputs:
        %   x: Audio vector
        %   Fs: Sampling frequency of audio
        function onsets = detectOnsets(x,Fs)
            
        end

    end

end