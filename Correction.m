classdef Correction < handle

    properties (SetAccess = immutable)
        deltaPhase      % Phase change
        deltaPeriod     % Period change
    end

    methods
        % Instantiates object and calculates correction
        function obj = Correction(hyp,onsets)
            
        end
    end

end